{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build nobuild",
            "type": "shell",
            "command": "zig cc -std=c11 -o nobuild.exe -ITools nobuild.c -fno-sanitize=undefined -target x86_64-windows",
            "problemMatcher": [
                "$msCompile"
            ],
            "group": "build"
        }
    ]
}